namespace Itmo.ObjectOrientedProgramming.Lab2.PersonalComputerConfigurator.Entities;

internal class Program
{
    public static void Main()
    {
        // Repository.Repository repository = new Repository.Repository();
        // IList<Cpu> cpuStorage = repository.СpuList;
        // IList<Bios> biosStorage = repository.BiosList;
        // IList<CoolingSystem> coolingSystemStorage = repository.CoolingSystems;
        // IList<Hdd> hddStorage = repository.Hdds;
        // IList<Motherboard> motherboardStorage = repository.Motherboards;
        // IList<PowerUnit> powerUnitStorage = repository.PowerUnits;
        // IList<Ram> ramStorage = repository.Rams;
        // IList<Ssd> ssdStorage = repository.SsdList;
        // IList<SystemCase> systemCaseStorage = repository.SystemCases;
        // IList<VideoCard> videoCardStorage = repository.VideoCards;
        // IList<WifiAdapter> wifiAdapterStorage = repository.WifiAdapters;
        // IList<Xmp> xmpStorage = repository.Xmps;
        // IList<Computer.Computer> computerStorage = repository.Computers;
        // IComputerBuilder computerBuilder = new ComputerBuilder();
        // Cpu cpu = _cpuStorage[0];
        // CpuBuilder cpuWithModifiedTdp = cpu.Clone();
        // cpuWithModifiedTdp.WithTDP(new TDP(200));
        // Motherboard motherboard = _motherboardStorage[2];
        // Bios bios = _biosStorage[0];
        // CoolingSystem coolingSystem = _coolingSystemStorage[0];
        // PowerUnit powerUnit = _powerUnitStorage[0];
        // Ram ram = _ramStorage[0];
        // Hdd hdd = _hddStorage[0];
        // SystemCase systemCase = _systemCaseStorage[1];
        // VideoCard videoCard = _videoCardStorage[0];
        // Xmp xmp = _xmpStorage[0];
        // WifiAdapter wifiAdapter = _wifiAdapterStorage[1];
        // (AddNotification? notification, DisclaimerOfWarrantyObligations? disclaimer, _, _) = computerBuilder.WithMotherBoard(motherboard).WithCpu(cpu).WithBios(bios).WithCoolingSystem(coolingSystem)
        //         .WithRam(ram).WithXmp(xmp).WithVideoCard(videoCard).WithSsd(null).WithHdd(hdd).WithSystemCase(systemCase)
        //         .WithPowerUnit(powerUnit).WithWifiAdapter(wifiAdapter).Build();
        // Console.WriteLine(motherboard3);
        // }
    }
}